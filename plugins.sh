# NPM Commands
npm install -g vmd yarn markdownlint-cli

# Vagrant plugins
# vagrant plugin install vagrant-bindfs

# PHP Composer
composer global require squizlabs/php_codesniffer

# Visual Studio Code
# Rebuild with: `code --list-extensions | xargs -L 1 echo code --install-extension`
code --install-extension Shan.code-settings-sync
